
import {
    Field,
    Input,
  } from "@fluentui/react-components";
  import * as React from "react";
  import PropTypes from 'prop-types';
  
  export const field = ({}) => {
      return (
            <Field>
    <Input />
  </Field>
      );
  };
  
  
 field.propTypes = {

  };
  
  field.defaultProps = {

  };
  