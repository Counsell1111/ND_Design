
import {
    Field,
    Input,
  } from "@fluentui/react-components";
  import React from 'react';
  import PropTypes from 'prop-types';
    
  export const Field = ({ }) => {
      return (
            <Field>
    <Input />
  </Field>
      );
  };
  
  
  Field.propTypes = {
  };
  
  Field.defaultProps = {
  };
  