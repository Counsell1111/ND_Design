import { Labels } from './Labels';

export default {
  title: 'Components/Labels',
  component: Labels,
  tags: ['autodocs'],
  argTypes: {
  
  },
};

export const Primary = {
    args: {
      },
};
